            <div class="system nn" id="from{{i}}">
                <p>
                    <span class="system_name">{{name}}</span><br>
                    <span class="system_coords">( {{x}} / {{y}} / {{z}} )</span> {{permit_start}}<span class="system_permit">needs permit</span>{{permit_end}}
                </p>
            </div>